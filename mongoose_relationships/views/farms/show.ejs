<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= farm.name %></title>
</head>
<body>
    <h1><%= farm.name %></h1>
    <ul>
        <li>City: <%= farm.city %> </li>
        <li>Email: <%= farm.email %> </li>
        <li>Products: 
            <ul>
                <% for(let product of farm.products){ %>
                    <li onclick="location.href='/products/<%= product._id %>'"><%= product.name %></li>
                <% } %>  
            </ul>
        </li>
    </ul>  
    
    <br>
    <form action="/farms/<%= farm._id %>?_method=DELETE" method="post">
        <button type="submit">Delete</button>
    </form>
    <a href="/farms/<%= farm.id %>/products/new">Add a product</a>
    <br>
    <a href="/farms">All Farms List</a>
</body>
</html>